#!/usr/bin/env bash
# Pick a hit (file[:line:...]) from stdin, open it in bat.
sel=$(fzf --ansi -d: --preview 'bat-at {1} {2}' --preview-window 'right,60%,border-left') || exit 0
IFS=: read -r file line _ <<<"$sel"
bat-at "$file" "$line"
