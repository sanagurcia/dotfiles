#!/usr/bin/env bash
# Which apps/packages a symbol appears in (top 10), plus total files.
rg -t src -T tst -lw "$1" . | cut -d/ -f2-4 | sort | uniq -c | sort -rn | head -10
printf '\n%4s files\n' "$(rg -t src -T tst -lw "$1" . | wc -l | tr -d ' ')"
