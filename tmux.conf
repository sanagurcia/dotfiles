set -g prefix C-Space
unbind C-b
bind C-Space send-prefix

# Let an escape sequence through to iTerm2 unchanged (tmux 3.3+). `theme` needs
# it both ways: to ask the terminal for its background, and to tell it to switch
# profile. Without this tmux swallows both and the theme never leaves this box.
set -g allow-passthrough on
